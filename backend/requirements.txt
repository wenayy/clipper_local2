fastapi
uvicorn[standard]
requests
pydantic
python-multipart
fonttools
sqlalchemy
# Billing provider. dodopayments ships checkout_sessions.create (dynamic amount
# for credit packs) and webhooks.unwrap (Standard Webhooks signature check).
# The [webhooks] extra pulls in `standardwebhooks`, which unwrap() requires.
dodopayments[webhooks]>=1.34.0

# Face-aware reframing for the Podcast template (app/pipeline/reframe.py).
# opencv carries the YuNet detector; the model file itself is an asset rather
# than a package -- see assets/models/README.md for the one-line fetch. Both
# are optional at runtime: with either missing the podcast frame falls back to
# a fixed letterbox instead of failing.
opencv-python-headless
SQLAlchemy==2.0.51
bcrypt==4.0.1
fastapi==0.128.8
httpx==0.28.1
passlib==1.7.4
pydantic==2.13.4
pydantic_core==2.46.4
python-jose==3.5.0
requests==2.32.5
uvicorn==0.39.0
psycopg2-binary>=2.9

# S3 API client, used for Cloudflare R2 (app/storage.py). Only needed when
# STORAGE_BACKEND=r2; the local filesystem driver imports nothing. Installed
# unconditionally so a deployment cannot be one missing package away from
# having nowhere to put the clips it just spent money rendering.
boto3

# Fetches source video and its metadata (app/pipeline/downloader.py shells out
# to the `yt-dlp` binary this package installs).
#
# It was missing here and present locally only because Homebrew had it, so the
# link preview worked on a laptop and returned 400 in about 5ms on the server --
# too fast to be a network failure, because the executable simply was not there.
#
# Unpinned on purpose: extractors break whenever a video site changes, and the
# fix always arrives as a new yt-dlp release. A pin here becomes an outage
# later.
yt-dlp

# Romanises Devanagari (and other non-Latin) caption text so every caption style
# renders without a font swap.
unidecode

